Use runif command to create matrix
 A <- matrix(runif(100, 1, 100), nrow =10)
 
 Check the determinanat of A using det()
 det(A)

Use solve() to find inverse of A
solve(A)

B<- matrix(1:1000, nrow=10)

Use ginv( ) to find pseudo-inverse as it is not a squared matrix
ginv (B)
